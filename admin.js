document.getElementById('boxForm').addEventListener('submit', function(event) {
    event.preventDefault();
    const boxName = document.getElementById('boxName').value;
    const boxGift = document.getElementById('boxGift').files[0];
    // 實現上傳盲盒的邏輯
});

document.getElementById('fetchMembers').addEventListener('click', function() {
    // 實現獲取會員列表的邏輯
});
